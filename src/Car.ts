import { AssetManager } from "./AssetManager";

export class Car {

    // private vars

    // event handlers

    // constructor
    constructor(stage:createjs.StageGL, assetManager:AssetManager) {
        // init

        //sprite
    }

    // --------------------- event handlers ----------------------------

    // ---------------------- gets / sets ------------------------------

    // --------------------- public meathods ---------------------------

    // --------------------- private meathods --------------------------
}